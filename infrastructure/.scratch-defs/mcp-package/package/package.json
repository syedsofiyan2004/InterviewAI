{
  "name": "sample-aws-pricing-calculator-mcp",
  "version": "1.3.0",
  "bin": "dist/mcp-server.js",
  "files": [
    "dist/mcp-server.js",
    "catalog/schema.json",
    "catalog/services/*.json"
  ],
  "description": "MCP server for creating, reading, updating AWS Pricing Calculator estimates through natural language.",
  "license": "MIT-0",
  "main": "mcp-server.js",
  "scripts": {
    "test": "npm run validate-catalog && SKIP_NETWORK=1 node --test test/*.test.js",
    "test:network": "npm run validate-catalog && node --test test/*.test.js",
    "test:roundtrip": "node --test test/catalog-roundtrip.test.js",
    "mcp": "node mcp-server.js",
    "validate-catalog": "node scripts/validate-catalog.js",
    "validate-catalog:cost": "node scripts/validate-verified-catalog.js",
    "build": "esbuild mcp-server.js --bundle --platform=node --outfile=dist/mcp-server.js --format=cjs --minify --external:@aws-sdk/client-dynamodb --external:@aws-sdk/lib-dynamodb && node scripts/emit-bundle-contract.js && rm -f dist/aws-calculator.zip && zip dist/aws-calculator.zip mcp-server.js lib/aws/aws-client.js lib/aws/fetch-resilience.js lib/aws/estimate-builder.js lib/aws/ec2.js package.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "express": "5.2.1"
  },
  "peerDependencies": {
    "@aws-sdk/client-dynamodb": ">=3.700.0",
    "@aws-sdk/lib-dynamodb": ">=3.700.0"
  },
  "peerDependenciesMeta": {
    "@aws-sdk/client-dynamodb": {
      "optional": true
    },
    "@aws-sdk/lib-dynamodb": {
      "optional": true
    }
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "3.1046.0",
    "@aws-sdk/lib-dynamodb": "3.1046.0",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "esbuild": "0.28.1",
    "playwright": "1.60.0"
  }
}
