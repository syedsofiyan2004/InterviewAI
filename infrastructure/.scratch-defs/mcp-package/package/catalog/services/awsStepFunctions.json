{
  "$schema": "../schema.json",
  "serviceCode": "awsStepFunctions",
  "displayName": "AWS Step Functions",
  "status": "verified",
  "lastVerifiedAt": "2026-07-23",
  "verifiedEstimateId": "a8add11e9464c5b4472e11c54495920ef5a620a4",
  "required": [],
  "traps": [
    "awsStepFunctions is a subServiceSelector parent. Call addService('stepFunctionStandard', ...) and/or addService('stepFunctionExpress', ...). EstimateBuilder merges children into a single parent envelope with estimateFor: awsStepFunctionGroups.",
    "Frequency unit MUST be one of perHour, perDay, perMonth — NOT millionPerMonth (which is valid elsewhere in the calculator but silently zeroes Step Functions pricing). Express the volume directly: 1M req/mo is value:'1000000', unit:'perMonth'.",
    "Express workflow memory field 'stateMachineDefinitionSize' uses fileSize shape with unit 'mb|NA' (not 'mb|month'). Valid sizes: mb, gb.",
    "Express workflow duration field ID contains spaces: 'Average duration of workflow'. Value is a plain numeric string in MILLISECONDS (min 100, max 300000 = the 5-minute Express Workflow cap), not seconds."
  ],
  "subServices": [
    {
      "serviceCode": "stepFunctionStandard",
      "estimateFor": "template_0",
      "required": [
        {
          "field": "numberOfExecutions",
          "hint": "Monthly workflow executions as a frequency",
          "shape": "{ value: '<n>', unit: 'perMonth' | 'perDay' | 'perHour' }",
          "example": {
            "value": "1000000",
            "unit": "perMonth"
          }
        },
        {
          "field": "stateTransition",
          "hint": "Average number of state transitions per workflow execution (numericInput — plain string)",
          "shape": "string",
          "example": "8"
        }
      ]
    },
    {
      "serviceCode": "stepFunctionExpress",
      "estimateFor": "template_0",
      "required": [
        {
          "field": "MonthlyWorkflowReqs",
          "hint": "Monthly workflow requests as a frequency",
          "shape": "{ value: '<n>', unit: 'perMonth' | 'perDay' | 'perHour' }",
          "example": {
            "value": "10000000",
            "unit": "perMonth"
          }
        },
        {
          "field": "Average duration of workflow",
          "hint": "Duration of each workflow execution in MILLISECONDS (numericInput — plain string). Min 100, max 300000 (5 min, the Express Workflow cap); default 500.",
          "shape": "string",
          "example": "100"
        },
        {
          "field": "stateMachineDefinitionSize",
          "hint": "Memory consumed by each workflow execution",
          "shape": "{ value: '<n>', unit: 'mb|NA' | 'gb|NA' }",
          "default": {
            "value": "64",
            "unit": "mb|NA"
          },
          "example": {
            "value": "64",
            "unit": "mb|NA"
          }
        }
      ]
    }
  ],
  "minimalConfig": {
    "stepFunctionStandard": {
      "region": "us-east-1",
      "description": "Standard Workflows",
      "numberOfExecutions": {
        "value": "1000000",
        "unit": "perMonth"
      },
      "stateTransition": "8"
    },
    "stepFunctionExpress": {
      "region": "us-east-1",
      "description": "Express Workflows",
      "MonthlyWorkflowReqs": {
        "value": "10000000",
        "unit": "perMonth"
      },
      "Average duration of workflow": "100",
      "stateMachineDefinitionSize": {
        "value": "64",
        "unit": "mb|NA"
      }
    }
  }
}
